<?xml version="1.0" encoding="UTF-8"?>
<project basedir="." default="help" name="Simkit">
	<description>
         Ant build file for the NPS Simkit and associated libraries. Third
         party jar files are not compiled from source here, but they are copied as
         necessary for the simkit distribution.  This buildfile was created and tested
         using Ant version 1.5.1 on the Mac OSX 10.3 platform, where it works from the
         command line as well as within the NetBeans IDE from Sun Microsystems. Suggestions
         and comments are welcome.
         
         It has also been tested on Linux with NetBeans 3.5.1 and JDK_1.4.2
        </description>
	
<!-- magic properties, do not change or delete -->

	<property name="build.compiler" value="modern"/>
	
<!-- CVS properties (this file) -->
	<property name="CVSId" value="$Id$"/>

<!-- Project directories             -->

	<property description="Root of the Simkit source tree." location="." name="dir.src"/>
	
	<property description="Root of the Simkit binary tree." location="${basedir}/build" name="dir.build"/>
	          
	<property description="Directory for built and copied jar files that will wind up in dir.dist." location="${dir.build}/lib" name="dir.build.lib"/>
	          
	<property description="Where the distribution files wind up." location="${basedir}/dist" name="dir.dist"/>
	          
	<property description="Location of Simkit's dependencies" location="${basedir}/lib" name="dir.lib"/>
	          
	<property description="Where all the XML extensions are." location="${dir.lib}/jwsdp-1.3" name="dir.lib.xml"/>
	          
	<property description="Location of class files resulting from operations of this build file." location="${dir.build}/classes" name="dir.build.classes"/>
	          
	<property description="Root of the javadoc tree for Simkit." location="${dir.build}/doc" name="dir.build.doc"/>
                  

<!--  === TARGET: help === -->
	<target name="help">
		<echo message="This is the Ant build file for the NPS Simkit and associated libraries. Third-party jar files are not compiled from source here, but they are copied as necessary for the simkit distribution.  This buildfile was created and tested using Ant version 1.5.1 on the Mac OSX 10.3 platform, where it works from the command line as well as within the NetBeans IDE version 3.5.1 from Sun Microsystems. Suggestions and comments should be forwarded to Kirk Stork (kirk@kastork.org), or posted on the Simkit discussion boards at http://diana.nps.navy.mil/simkit.  Work on integrating Ant and this buildfile within Apple's XCode IDE is in progress."/>
    </target>

<!--  === TARGET: init === -->
	<target description="Preparatory actions required for all targets." name="init">
	    <tstamp/>
		<echo message="Build started on ${DSTAMP} at ${TSTAMP}"/>
		<echo message="Buildfile version ${CVSId}"/>
		<echo message="Base directory: ${basedir}"/>
		<echo message="Source directory: ${dir.src}"/>
		<echo message="Build directory: ${dir.build}"/>
	</target>
	
<!--  === TARGET: build.init === -->
	<target depends="init" description="Preparatory actions for compilation and documentation targets" name="build.init">
		<mkdir dir="${dir.build}"/>
		<mkdir dir="${dir.build.lib}"/>
		<mkdir dir="${dir.build.classes}"/>
                <mkdir dir="${dir.build.doc}"/>
	</target>
	
<!--  === TARGET: clean === -->
    <target depends="init" description="Deletes build directory, javadoc directory and dist directory" name="clean">
        <delete dir="${dir.build}"/>
        <delete dir="${dir.dist}"/>
    </target>

<!--  === TARGET: compile === -->
    <target depends="build.init" description="Compile the Simkit classes and copy resource files to the binary tree." name="compile">
        <javac destdir="${dir.build.classes}">
        	<src path="${dir.src}/simkit"/>
        	<src path="${dir.src}/graph"/>
        </javac>
        
        <copy todir="${dir.build.classes}">
            <fileset dir="${dir.src}">
            	<include name="**/*.txt"/>
            	<include name="**/*.png"/>
            	<include name="**/*.gif"/>
            	<include name="**/*.jpg"/>
        	<include name="*.txt"/>
        	<exclude name="build/"/>
        	<include name="**/*.xml"/>
        	<include name="**/*.dtd"/>
            </fileset>
        </copy>
        <copy todir="${dir.build}">
            <fileset dir="${dir.src}">
                <include name="*.txt" />
            </fileset>
        </copy>
    </target>
    
<!--  === TARGET: jar === -->
    <target depends="compile" name="jar">
        <jar basedir="${dir.build.classes}" compress="true" excludes="*/test/*" jarfile="${dir.build.lib}/simkit.jar"/>
    </target>

<!--  === TARGET: doc === -->
    <target depends="build.init" description="Create the javadoc for Simkit." name="doc">
        
        <javadoc access="private" author="true" destdir="${dir.build.doc}" excludepackagenames="*.test.*" sourcepath="${dir.src}" version="true">
            <package name="simkit"/>
            <package name="simkit.examples"/>
            <package name="simkit.random"/>
            <package name="simkit.smd"/>
            <package name="simkit.smdx"/>
            <package name="simkit.stat"/>
            <package name="simkit.util"/>
            <package name="simkit.xml"/>
        </javadoc>
    </target>
    
<!-- === TARGET: jar.source === -->
    <target depends="build.init" name="jar.source">
        <jar excludes="**/*.class" jarfile="${dir.build}/simkit-src.zip">
            <fileset dir="${dir.src}">
                <exclude name="build/**/*"/>
                <exclude name="dist/**/*"/>
                <exclude name="CVS/**.*"/>
                <exclude name="**/.nb*" />
                <exclude name="Simkitlocales/**/*" />
                <exclude name="Simkit_Build_Output/**/*" />
                <exclude name="**/*.class"/>
                <exclude name ="lib/**.*" />
            </fileset>
            
        </jar>
    </target>

    
<!-- === TARGET: dist === -->

	<target depends="jar, doc, jar.source" name="dist">
	    <mkdir dir="${dir.dist}"/>
<!--	    <mkdir dir="${dir.dist}/doc"/> -->
	    <mkdir dir="${dir.dist}/lib"/>
	    <mkdir dir="${dir.dist}/gnu"/>
		<copy todir="${dir.dist}/lib">
			<fileset dir="${dir.build.lib}"/>
		</copy>
<!--  -->              
		<copy todir="${dir.dist}/doc">
			<fileset dir="${dir.build.doc}"/>
		</copy>
<!-- -->
                <jar jarfile="${dir.dist}/simkit-doc.zip">
                    <fileset dir="${dir.build}/doc" />
                </jar>
		<copy todir="${dir.dist}/lib">
			<fileset dir="${dir.lib}"/>
		</copy>
                <copy file="${dir.build}/simkit-src.zip" todir="${dir.dist}"/>
                <copy todir="${dir.dist}/gnu">
                    <fileset dir="${dir.src}/gnu"/>
                </copy>
                <copy todir="${dir.dist}">
                    <fileset dir="${dir.build}">
                        <include name="*.txt"/>
                    </fileset>
                </copy>
	</target>
</project>
